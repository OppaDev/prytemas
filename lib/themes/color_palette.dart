import 'package:flutter/material.dart';

// Colores Comunes
const Color appPrimaryBlue = Color(0xFF3B82F6); // Azul primario de las imágenes
const Color appLightBlue = Color(0xFF60A5FA);   // Azul claro para acentos
const Color appDarkBlue = Color(0xFF2563EB);    // Azul oscuro para interacciones

// --- Colores Tema Oscuro ---
const Color darkBackgroundColor = Color(0xFF0A0F1A); // Azul marino muy oscuro (fondo principal)
const Color darkPrimaryColor = appPrimaryBlue;       // Azul para botones y elementos destacados
const Color darkSecondaryColor = appLightBlue;     // Azul más claro para acentos secundarios
const Color darkSurfaceColor = Color(0xFF1E293B);    // Azul pizarra oscuro (para Cards, Dialogs)
const Color darkTextColor = Colors.white;            // Texto principal blanco
const Color darkSecondaryTextColor = Color(0xFFCBD5E1); // Gris pizarra claro (texto secundario)
const Color darkIconColor = Colors.white;            // Iconos blancos
const Color darkHintTextColor = Color(0xFF94A3B8);   // Gris pizarra (para placeholders, hints)
const Color darkBorderColor = Color(0xFF334155);   // Gris pizarra más oscuro (para bordes, divisores)
const Color darkChipBackgroundColor = Color(0xFF334155); // Fondo para los botones tipo chip

// --- Colores Tema Claro ---
const Color lightBackgroundColor = Color(0xFFF8F9FA); // Gris muy claro (casi blanco, fondo principal)
const Color lightPrimaryColor = appDarkBlue;        // Azul oscuro para botones y elementos destacados
const Color lightSecondaryColor = appPrimaryBlue;   // Azul primario para acentos secundarios
const Color lightSurfaceColor = Colors.white;        // Blanco (para Cards, Dialogs)
const Color lightTextColor = Color(0xFF111827);      // Gris muy oscuro (casi negro, texto principal)
const Color lightSecondaryTextColor = Color(0xFF4B5563); // Gris oscuro (texto secundario)
const Color lightIconColor = Color(0xFF111827);      // Iconos oscuros
const Color lightHintTextColor = Color(0xFF6B7280); // Gris medio (para placeholders, hints)
const Color lightBorderColor = Color(0xFFD1D5DB);   // Gris claro (para bordes, divisores)
const Color lightChipBackgroundColor = Color(0xFFE5E7EB); // Fondo para los botones tipo chip (gris claro)